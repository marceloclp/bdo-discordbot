const map_type_to_icon  = require('./map_type_to_icon')
const map_grade_to_icon = require('./map_grade_to_icon')

const parseTable  = require('./parseTable')
const parseList   = require('./parseList')
const parseHeader = require('./parseHeader')
const parseIcon   = require('./parseIcon')

module.exports = {
    map_type_to_icon,
    map_grade_to_icon,
    parseTable,
    parseList,
    parseHeader,
    parseIcon,
}